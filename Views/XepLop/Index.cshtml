@model IEnumerable<QLY_HOCSINH.Models.XepLop>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lop)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Hs.HsId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lop.LopId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.XeplopId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.XeplopId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.XeplopId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
