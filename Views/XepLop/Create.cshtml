@model QLY_HOCSINH.Models.XepLop

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>XepLop</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="LopId" class="control-label"></label>
                <select asp-for="LopId" class ="form-control" asp-items="ViewBag.LopId"></select>
            </div>
            <div class="form-group">
                <label asp-for="HsId" class="control-label"></label>
                <select asp-for="HsId" class ="form-control" asp-items="ViewBag.HsId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
